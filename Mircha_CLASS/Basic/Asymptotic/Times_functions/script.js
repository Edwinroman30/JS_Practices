/* const timer = 
setTimeout(() => {
    console.log('Hello word')
}, 1000);

clearTimeout(timer)

let int = 0
const timer2 = setInterval(() =>{
    console.log(`BBP000${int++}`)
},1000)


clearInterval(timer2)

const Prueba = (name,callback) =>{
    console.log(`Hello i'm ${name}`)

    callback(name)
}

Prueba("Edwin", (n) => {
    console.log(`A special greating for my brother ${n}`)
})

Prueba("Juan chivos", () =>{
    console.log(`this men ${name} is doing ${3+3}`)
}) */

//Funcion que se ejecuta cada cierto intervalo de tiempo.
    // * setinterval(function(),1000) *//
//Funcion que se ejecuta solo cierto intervalo de tiempo.
    // * settimeout(function(),1000) *//
